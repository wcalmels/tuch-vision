# Example profiles

| Folder | Role |
|--------|------|
| `tth/` | **Example only** — TTH OCR soft priors. Not loaded by default. |

Core `tuch-vision` is manuscript-agnostic. Put your own hints in `manuscript.toml`.
